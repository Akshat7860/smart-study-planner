# Development mein blank rakho — Vite proxy /api → localhost:5000 handle karta hai
# Production (Vercel/Netlify) mein apna backend URL daalo:
# VITE_API_URL=https://your-app.onrender.com/api
VITE_API_URL=
